// const para = document.querySelector('p');

// para.addEventListener('click', updateName);

// function updateName() {
//   const name = prompt('Enter a new name');
//   para.textContent = `Player 1: `;
// }


let grid = document.querySelector('div[class="grid-wrapper"]');

// document.addEventListener('DOMContentLoaded', () => {
//     function createParagraph(){
//         const para = document.createElement('p');
//         para.textContent = 'You Clicked the Button';
//         // this is likely unsafe as we don't know if the grid is always there
//         grid.appendChild(para);
//     }
//     const buttons = document.querySelectorAll('button');
//     buttons.forEach((button)=>button.addEventListener('click', createParagraph));
// });